# Private licensing tooling - keys/secrets, never commit.
/licensing/
license_client.py

# Paid Full Auto source stays local/protected. Public repo may contain the
# paywall UI, docs, templates, and build hooks, but not the paid implementation
# source or license client.
full_auto.py
DESIGN_FULL_AUTO.md
PAID_FUNCTION.md
# Local-only Full Auto template images that should not be published.
templates/**/full_auto/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# PyInstaller artifacts
build/
dist/
*.spec

# Release distribution (built output - not source)
FAFE_dist/
FAFE/
Output/
FAFE v*/
FAFE.v*.zip
*.zip
*.log
MicrosoftEdgeWebview2Setup.exe

# Local agent/editor + scratch
.claude/
.qwen/
docs/superpowers/
AGENTS.md
CLAUDE.md
Codex.md
patchnote.md
QWEN.md
debug/
roi_preview.png
webui_poc/
design_handoff_full_auto/

# User local config (generated by build_app.bat into FAFE_dist/)
config.json

# Bug-report bundles (generated at runtime by report.py)
reports/

# runtime diagnostics
fafe_diag.log
fafe_diag.log.prev
build_log.txt
license.dat

# User-captured custom templates (never commit - user data, any language)
templates/**/custom/
templates/*/mastery_full/mastery_presets.json
compiled/
nuitka_build/

# Forza game cinematic XML + reference mod (copyrighted third-party content - dev references only, never commit)
assets/pgc*.xml
assets/*homespace*.xml
fh-less-anims-*.zip
